import type { WidgetPreset } from "./types";

export const minimalPreset: WidgetPreset = {
  id: "minimal",
  name: "Minimal",
  description: "Subtle borders and spacious whitespace",
  overrides: {},
};
